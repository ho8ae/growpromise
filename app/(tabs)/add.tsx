import React from 'react';
import {StyleSheet, View} from 'react-native';

interface addProps {

}

const add = ({}: addProps) => {
  return (
    <View></View>
  )
}

const styles = StyleSheet.create({});

export default add;