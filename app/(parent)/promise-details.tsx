import React from 'react';
import {StyleSheet, View} from 'react-native';

interface PromiseDetailsProps {

}

const PromiseDetails = ({}: PromiseDetailsProps) => {
  return (
    <View></View>
  )
}

const styles = StyleSheet.create({});

export default PromiseDetails;